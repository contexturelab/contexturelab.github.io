# frozen_string_literal: true

source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "kramdown-parser-gfm"
gem "webrick"
gem "jekyll-feed"
gem "jekyll-seo-tag"

# This is for local development only
# GitHub Pages will use remote_theme: riggraz/no-style-please

# Note: If you encounter eventmachine installation issues on newer Ruby/macOS,
# you can run Jekyll without live-reload using:
# bundle exec jekyll serve --no-watch --skip-initial-build
# Or use: jekyll serve --livereload false
